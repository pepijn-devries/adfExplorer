# Close all virtual devices

Close all virtual devices that are currently open. This function is
useful when you have multiple devices opened at the same time.

## Usage

``` r
close_all_devices()
```

## Value

Returns `NULL` invisibly.
